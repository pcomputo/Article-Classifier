import unsafe_flags

article = fileUsage.get_input()

tags = fileUsage.get_tag(article)
print tags

flag = fileUsage.is_unsafe(article)
if (flag):
  print "This article has been marked as unsafe!"
else:
  print "This article is safe and can be processed!"
